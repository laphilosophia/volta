import { ComponentDefinition } from '../../core/types'; import {
{{camelCase name}}Schema } from '../headless/{{pascalCase name}}/schema'; export const
{{camelCase name}}Definition: ComponentDefinition = { type: '{{pascalCase name}}', name: '{{pascalCase
  name
}}', group: 'General', icon: 'Square', // Import from lucide-react if needed schema:
{{camelCase name}}Schema, defaultProps: { // defaults }, };
